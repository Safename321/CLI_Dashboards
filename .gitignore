# Reference material — not part of the new build (§19)
_v1.24L_source/
CLIDashboards_v1.24L.zip

# Deps & build output
node_modules/
dist/
ghpages-out/

# Secrets — never commit (§3.9)
.env
.env.local
*.local

# Operator-local runtime files
.ntfy-topic
.autorun-enabled

# all env files incl. editor backups (.env.save, .env.local) — never commit secrets
.env*
!.env.example

.vercel

# Planning/working docs — kept locally, excluded from the GitHub repo to keep it clean.
# (src/mentor/system-prompt.md is intentionally NOT ignored — it's loaded at runtime.)
/ARCHITECTURE.md
/PARITY_CHECKLIST.md
/PROGRESS.md
/PUBLISHING.md
/README.md
/REWRITE_BRIEF.md
/WORKDAY_IMPL_BRIEF.md
/WORKDAY_PROGRESS.md
/WorkDay/

.vercel
